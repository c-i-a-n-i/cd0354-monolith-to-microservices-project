apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-reverseproxy
  labels:
    app: udagram-reverseproxy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: udagram-reverseproxy
  template:
    metadata:
      labels:
        app: udagram-reverseproxy
    spec:
      containers:
        - name: udagram-reverseproxy
          image: ifani/udagram-reverseproxy:v1
          ports:
            - containerPort: 80
          env:
            - name: POSTGRES_USERNAME
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
            - name: POSTGRES_HOST
              value: postgres.cdortfk9dtk0.us-east-1.rds.amazonaws.com
            - name: POSTGRES_DB
              value: postgres
            - name: AWS_BUCKET
              value: udagram-308078610002-prod
            - name: AWS_ACCESS_KEY_ID
              value: AKIAUPOXKQZJANQXA4OZ
            - name: AWS_SECRET_ACCESS_KEY
              value: 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
            - name: AWS_REGION
              value: us-east-1
            - name: AWS_PROFILE
              value: default
            - name: JWT_SECRET
              value: testing
            - name: URL
              value: http://localhost:8100
